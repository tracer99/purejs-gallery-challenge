/*
we import these modules to register the custom components.
Linters will throw errors because we never actual use these modules in code.
*/
import Binding from './binding.js';
import ImageGallery from './components/image-gallery/image-gallery.js';